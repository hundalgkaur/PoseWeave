/// Hardcoded demo account for the gated login (no backend). Shown on the login
/// screen so the demo APK is usable by anyone.
class AuthConstants {
  const AuthConstants._();

  static const String demoEmail = 'test@poseweave.app';
  static const String demoPassword = 'test1234';
}
